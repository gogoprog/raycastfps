package;

class Context {
    public var renderer = new Renderer();
    public var cameraTransform = new Transform();
    public var textureManager = new TextureManager();
    public var level = new Level();

    public function new() {
    }
}
